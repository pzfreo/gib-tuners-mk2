## Create PR
1. Run all tests: `python -m pytest tests/ -x`
2. If tests pass, commit with a descriptive message
3. Push to current branch
4. Create PR with summary of changes
5. Never force-push or rebase without asking
